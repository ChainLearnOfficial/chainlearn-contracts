[package]
name = "chainlearn-shared"
version = "0.1.0"
edition = "2021"
description = "Shared types and utilities for ChainLearn contracts"
publish = false

[dependencies]
soroban-sdk = { workspace = true }
